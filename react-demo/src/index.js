import React from "react"
import ReactDOM from "react-dom"
import AppComponent from "./app"
const root=ReactDOM.createRoot(document.getElementById('root'))
root.render(<AppComponent fname="john" role="admin"></AppComponent>)